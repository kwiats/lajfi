from motivation.models import UserAchievement

def check_category_achievement(user, achievement, conditions):
    # Implement logic to check if tasks across different categories are completed
    pass
